[tool.poetry]
name = "KAGGLE-CHALLENGE-2024"
version = "0.1.0"
description = ""
authors = ["Iryna Yavorska", "Doug Ollerenshaw"]
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"